#include "common.h"

float4 main(p_screen I) : SV_Target
{
	float3	col = s_image.Load(int3(I.hpos.xy, 0), 0);
			col *= 1.4f - (1.4f * saturate(distance(I.tc0, float2(0.5f, 0.5f))));

	return float4(col, 1.0f);
}